$TTL	604800
{{ domain_name }}.	IN	SOA	{{ domain_name }}. ssaart.{{ domain_name }}. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;

{% for srvr in groups['dns'] %}
{{ domain_name }}.	IN	NS	{{ srvr }}.{{ domain_name }}.
{% endfor %}

{% for srvr in hostvars %}
{{ srvr }}	IN	A	{{ hostvars[srvr]['ansible_default_ipv4']['address'] }}
{% endfor %}
