---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init
      - dns
      - bind
    tags: init

  - name: full reset part 1
    hosts: srvr
    become: yes
    roles:
      - full_reset
    tags:
      - never
      - reset