---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init
    tags: init

  - name: pre-ceph
    hosts: hp_servers
    roles:
      - python
      - bind
      - dns
      - ssh
    tags: pre-ceph

#  - name: ceph
#    hosts: hp_servers
#    roles:
#      - ceph
#    tags: ceph

  - name: full reset part 1
    hosts: srvr
    become: yes
    roles:
      - full_reset
    tags:
      - never
      - reset